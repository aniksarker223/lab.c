#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    long long a = 0, b = 1, sum = 0;
    for (int i = 1; i <= n; i++) {
        long long next = a + b;
        a = b;
        b = next;
        if (i % 2 == 0) sum += a;
    }

    printf("%lld", sum);
    return 0;
}
