#include <stdio.h>

int main() {
    char s[200], out[200];
    int i, j=0;

    printf("Enter string: ");
    gets(s);

    for(i=0; s[i]; i++){
        if(!(s[i]>='0' && s[i]<='9'))
            out[j++] = s[i];
    }

    out[j] = '\0';
    printf("Output: %s\n", out);

    return 0;
}
