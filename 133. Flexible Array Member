#include <stdio.h>
#include <stdlib.h>
struct arr{ int n; int a[]; };
int main(){
    int n; scanf("%d",&n);
    struct arr *p = malloc(sizeof(*p)+n*sizeof(int));
    p->n=n;
    for(int i=0;i<n;i++) scanf("%d",&p->a[i]);
    for(int i=0;i<n;i++) printf("%d ",p->a[i]);
    return 0;
}
