#include <stdio.h>

int main() {
    char s[200];
    int i;

    printf("Enter string: ");
    gets(s);

    for(i=0; s[i]; i++){
        if(s[i]==' ')
            s[i]='-';
    }

    printf("Output: %s\n", s);

    return 0;
}
