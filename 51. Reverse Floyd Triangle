#include <stdio.h>

int main() {
    int n, k = 1;
    scanf("%d", &n);

    int total = (n * (n + 1)) / 2;
    k = total;

    for (int i = n; i >= 1; i--) {
        for (int j = 1; j <= i; j++)
            printf("%d ", k--);
        printf("\n");
    }

    return 0;
}
