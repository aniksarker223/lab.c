#include <stdio.h>

int main() {
    char s[200];
    int i, words = 1;

    printf("Enter a sentence: ");
    gets(s);

    for (i = 0; s[i] != '\0'; i++) {
        if (s[i] == ' ')
            words++;
    }

    printf("Total words = %d\n", words);

    return 0;
}
