#include <stdio.h>

int main() {
    char s[100];
    int i, flag = 0;

    printf("Enter string: ");
    gets(s);

    for(i=0; s[i]; i++){
        if(s[i] < '0' || s[i] > '9'){
            flag = 1;
            break;
        }
    }

    if(flag == 0) printf("Only digits\n");
    else printf("Not only digits\n");

    return 0;
}
