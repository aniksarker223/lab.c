#include <stdio.h>
#include <string.h>
int main() {
    char a[100], b[100];
    gets(a); gets(b);
    int x = strcmp(a,b);
    if(x==0) printf("Equal");
    else if(x<0) printf("A < B");
    else printf("A > B");
    return 0;
}
